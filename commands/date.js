const Client = require('discord.js').Client;

module.exports = {
    name: "date",
    aliases: [],
    description: "checks the current japanese time",
    execute(message, arguments){
        return "";
    }

}